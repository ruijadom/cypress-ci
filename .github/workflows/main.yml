name: Generate Cypress Report
on: [push]

jobs:
  end_to_end_tests:
    name: End to end Tests
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Cypress install
        uses: cypress-io/github-action@v2
        with:
          runTests: false

      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          start: npm run int-test
          wait-on: "http://localhost:3001"
          browser: chrome
          parallel: true
